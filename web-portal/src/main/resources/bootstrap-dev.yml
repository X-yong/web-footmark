spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: web-config
      profile: dev
      label: master
      failFast: true
management:
  security:
    enabled: false  #忽略权限拦截
eureka:
  client:
    service-url:
      defaultZone: http://localhost:11111/eureka/
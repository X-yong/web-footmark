spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: web-config
      profile: dev
      label: master
      failFast: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:11111/eureka/